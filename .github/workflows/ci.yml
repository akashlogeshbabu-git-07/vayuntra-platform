name: Vayuntra CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    name: Platform Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Done
        run: echo "Vayuntra CI passed"
