# Vayuntra Agent — Python Dependencies
# Install: pip install -r requirements.txt

# Core runtime
psutil==5.9.8
structlog==24.1.0
pyyaml==6.0.1
httpx==0.27.0
cryptography==42.0.5

# Offline telemetry buffer
# (uses stdlib sqlite3 — no extra dep)

# Filesystem monitoring (optional — agent degrades gracefully without it)
watchdog==4.0.0

# ML model inference (optional — falls back to heuristic without it)
numpy==1.26.4
scikit-learn==1.4.2
joblib==1.3.2

# Windows-only (installed only on Windows)
# pywin32==306  # Uncomment on Windows agents

# Dev/test only
pytest==8.1.1
pytest-asyncio==0.23.6
